NixOS setup with vs codium for python, kubernetes & yaml development support.
Configures vpn access with pi-hole as dns and k3s for container orchestration intended for a base of a simple internal development platrofm available over the vpn to multiple people/employees.

## Usage

```bash
sudo nixos-rebuild switch --flake /etc/nixos#nixos
```
